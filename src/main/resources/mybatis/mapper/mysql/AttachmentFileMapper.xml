<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dage.rent.DAO.mysql.AttachmentFileDAO">

    <insert id="insert" parameterType="com.dage.rent.DTO.AttachmentFileDTO" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO attachment_file (
            original_filename, stored_filename, stored_path, section, file_size, content_type, encrypted, contract_seq
        ) VALUES (
            #{originalFilename}, #{storedFilename}, #{storedPath}, #{section}, #{fileSize}, #{contentType}, #{encrypted}, #{contractSeq}
        )
    </insert>

    <select id="findById" resultType="com.dage.rent.DTO.AttachmentFileDTO">
        SELECT id, original_filename AS originalFilename, stored_filename AS storedFilename, stored_path AS storedPath,
               section, file_size AS fileSize, content_type AS contentType, encrypted, contract_seq AS contractSeq, created_at AS createdAt
        FROM attachment_file
        WHERE id = #{id}
    </select>

    <select id="findByIds" resultType="com.dage.rent.DTO.AttachmentFileDTO">
        SELECT id, original_filename AS originalFilename, stored_filename AS storedFilename, stored_path AS storedPath,
               section, file_size AS fileSize, content_type AS contentType, encrypted, contract_seq AS contractSeq, created_at AS createdAt
        FROM attachment_file
        WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>

    <update id="updateContractSeqByIds">
        UPDATE attachment_file
        SET contract_seq = #{contractSeq}
        WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </update>

    <delete id="deleteById">
        DELETE FROM attachment_file WHERE id = #{id}
    </delete>
</mapper>
